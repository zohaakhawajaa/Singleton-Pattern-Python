# Singleton-Pattern-Python
A simple implementation of the Singleton design pattern in Python. Ensures a class has only one instance and provides a global access point. Useful for managing shared resources like configuration, logging, or database connections.
